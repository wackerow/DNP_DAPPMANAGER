import { expect } from "chai";

describe("Watchers > chains > utils", () => {
  describe("get Api URL", () => {
    it("Should parse an OpenEthereum state when syncing from snapshot", () => {});
  });
});
